version: "3.6"

services:
  rexams:
    image: kenjis/rexams:latest
    volumes:
      - type: bind
        source: ~/Courses/Problems/macro
        target: /home/rstudio/exams/problems
      - type: bind
        source: ./students.csv
        target: /home/rstudio/students.csv
    ports:
      - 8787:8787
    environment:
      PASSWORD: tomato
